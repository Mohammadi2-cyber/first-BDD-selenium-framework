
# General configuration

# Environment (DEV || TEST || STAGE || PROD)
env=DEV

#Browser configuration
browser=CHROME

# Headless mode (TRUE || FALSE)
isHeadless=FALSE

# Wait configuration (TRUE || FALSE)
#Enable or disable global wait
waitEnabled=FALSE

#Wait in seconds between each action
waitInSeconds=10